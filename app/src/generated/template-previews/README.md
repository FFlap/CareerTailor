# Template previews

These SVGs are generated assets and are intentionally committed so normal
development and deployment builds do not depend on external font or Typst
package services.

After changing a built-in template or its bundled assets, regenerate them with:

```sh
npm run previews:generate
```
